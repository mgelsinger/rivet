// ── Session persistence ───────────────────────────────────────────────────────
//
// Reads and writes `%APPDATA%\Rivet\session.json`.
// No `unsafe` — pure safe Rust + serde_json (added in Phase 4c).
//
// Phase 4a: placeholder module so `mod session;` compiles.
// Phase 4c: real implementation.
